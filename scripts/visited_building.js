
var visitedBuildings = [];

/**
 * var bl = document.querySelector('#bl');
for (var i = 0; i < visitedBuildings.length; i++) {
    bl.innerHTML += '<ul>' + '<li>' + visitedBuildings[i] + '</li>' + '</ul>';
}
 */

